<h1>All threads</h1>

{{#each threads}}
  <article>
    <h3><a href="/post/{{this._id}}/{{this.title}}">{{this.title}}</a></h3>
    <p>posted on: {{this.date}}</p>
    <p>author: <a href="/profile/{{this.author.username}}">{{this.author.username}}</a></p>
    <p> Last answer: {{this.lastAnswerDate}}</p>
    <p><span>Views: {{this.views}} </span><span>Likes: {{this.likes}} </span></p>
  </article>
{{else}}
  <p>No threads to show.</p>
{{/each}}

<div class="font-3x">
  {{#if hasPrevPage}}
    <a href="/list?page={{prevPage}}">&lt;&lt;</a>
    &nbsp; &nbsp; &nbsp;
  {{/if}}
  {{#if hasNextPage}}
    <a href="/list?page={{nextPage}}">&gt;&gt;</a>
  {{/if}}
</div>